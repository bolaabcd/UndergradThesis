Pearl mentions that the probability of necessity is in general not identifiable (it's the chance of $Y=0$ if $X$ were zero given that both are $1$, if zero means \q{didn't happen} and one means \q{happenned}).


